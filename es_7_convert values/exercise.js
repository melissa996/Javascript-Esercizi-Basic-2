let hello = 'Ciao';     // number
let age = 18;       // boolean
let isGraduated = false;     // string

console.log (Number(hello))
console.log (Boolean(age))
console.log (String(isGraduated))

// let hello = 'Ciao'; --> non è possibile convertire una stringa in una stringa di numeri, 
//il risultato sarà Nun (perchè state considerando qualcosa che non dovrebbe essere un numero)